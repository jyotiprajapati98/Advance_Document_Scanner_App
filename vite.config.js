import { defineConfig } from 'vite';

export default defineConfig({
  root: './',  // Set the root directory if necessary
  plugins: [],
  server: {
    port: 5173,  // Ensure you're running on the correct port
  },
});
